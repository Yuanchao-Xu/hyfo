# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: required
env:
  global:
    - NOT_CRAN = true
install:
  - ./travis-tool.sh install_r rgdal
  - ./travis-tool.sh install_r ncdf

before_install: echo "options(repos = c(CRAN='http://cran.rstudio.com'))" > ~/.Rprofile